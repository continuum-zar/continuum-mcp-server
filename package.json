{
  "name": "continuum-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Continuum — fetch task context for Cursor agents",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "continuum-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/continuum-zar/continuum-mcp-server.git"
  }
}
